/** Wikimedia Commons photos, chosen for verifiable real-world subject match
 * (file titles describe content) rather than guessed stock-photo IDs. */
export function wm(commonsThumbUrl: string) {
  return commonsThumbUrl
}
